import { Redirect } from 'expo-router';

export default function Index() {
  // This file acts as the entry point for the root '/' route.
  // The global redirect logic in app/_layout.jsx will handle 
  // moving the user to either /(auth)/login or /(tabs)/home.
  return null;
}
